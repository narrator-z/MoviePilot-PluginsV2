# 2026-08-01 工作日志

## NeoDBSource v1.0.4：详情页加演员表 + 相似推荐
- 需求：用户回复「加」，延续上次「详情页加演员表/相似推荐」提议。
- 实现：
  - `neodbhelper.py` 新增 `get_credit`/`async_get_credit`（免 token）、`get_similar`/`async_get_similar`（需 Bearer）、`credit_to_actors`、`similar_to_text`。
  - `__init__.py`：`_recognize_by_neodb_id` 内拉取 credit 填 `mi.actors`；有 token 时拉 similar 注入 `mi.overview`；新增配置项 `neodb_token`（密码框）；`plugin_version` 升 1.0.4。
  - `package.v2.json` NeoDBSource `version` 升 1.0.4（双字段同步）。
- 关键事实（容器内实测）：`MediaInfo.actors` 是 `List[dict]`（name/character/profile_path），详情页原生渲染；`schemas.MediaInfo` **无** recommend/similar 字段，故相似只能注入 overview。credit 端点免 token 返回 `data[]`（person.display_name + character_name + cover_image_url 全 URL）；similar 端点无 token 返回 401。
- 部署：scp 双路径 docker cp + 清缓存 + `docker restart moviepilot-v2`，冷启动后日志确认 `加载插件：NeoDBSource 版本：1.0.4` 无失败。
- 验证：容器内实例化插件调 `_recognize_by_neodb_id`，演员表 20 条正常填充；`similar_to_text` 文本块正确；空 token 安全跳过。
- 待用户：相似推荐需用户去 NeoDB 设置-开发者-创建应用 OAuth 授权拿 access_token 填进插件配置，否则只显示演员表。
