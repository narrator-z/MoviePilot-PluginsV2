# 2026-07-16

## 调试：JackettExtend 搜索返回空
- 环境：用户 NAS（飞牛 fnOS）上 MoviePilot 与 Jackett 均为独立应用；插件 jackettextend 已装，搜索总为空。
- 已排除的假设（已验证）：
  - Jackett 路由为 `api/v2.0/indexers/{id}/results/torznab/{ignored?}`，`/api` 后缀可选 → 插件 `.../results/torznab/` 合法，非 bug。
  - MoviePilot `StringUtils.get_url_domain("jackett_extend.<id>")` 返回原串，`split(".")[-1]` 正确得到索引器 id，URL 拼装无误。
- 用户确认：查看数据里索引器列表**有内容**、Jackett 地址填的是局域网 IP → 网络/host/apikey/可达性均正常，`get_indexers()` 成功。
- 现状：根因收窄到两个搜索专属疑点，待用户跑诊断脚本确认：
  - A. `cat=2000,5000` 参数把结果过滤空（列表接口不带 cat 故正常）。
  - B. MoviePilot 认证闸门在搜索时拦截（插件 monkeypatch `SitesHelper.check_user`/`auth_level` 及注入 `UserSiteAuthParams` 可能在用户版本未生效；git 历史显示此绕过反复修过）。
- 交付：`diagnose_jackett.py`（stdlib，无需依赖）复刻插件请求，对比带/不带 cat 的 <item> 数量；并请用户查日志是否出现"用户未认证/无法使用站点功能"。
- 下一步：等用户回传脚本输出 + 日志关键词，再针对性修复（A=删 cat 一行；B=加固认证绕过）。根因确认前不改代码。
